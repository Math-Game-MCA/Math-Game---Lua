local id

function on_message(self, message_id, message, sender)
	 if message_id == hash("transition") then
 		local component = "#transitionfactory"
		id = factory.create(component, vmath.vector3(-500, 360, 1))
	end
	if message_id == hash("done") then
		go.delete(id)
	end
end